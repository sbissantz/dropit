% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dropit.R
\name{dropit}
\alias{dropit}
\title{Drop Items from a Psychometric Scale}
\usage{
dropit(
  data = data.frame(),
  partition = NULL,
  n_drop = 1L,
  direction = c("tail", "head"),
  criterion = c("alpha", "lambda"),
  approach = c("oneshot", "greedy"),
  output_type = c("names", "subset", "both", "debug"),
  alpha_metric = c("raw_alpha", "std.alpha", "G6(smc)", "average_r", "S/N", "alpha se",
    "var.r", "med.r"),
  alpha_args = list(),
  measurement_model = NULL,
  target_factor = NULL,
  lambda_metric = c("est", "std", "std.lv", "std.nox", "std.all"),
  cfa_args = list(),
  verbose = TRUE
)
}
\arguments{
\item{data}{A \code{data.frame} of item responses
(rows = respondents, columns = items). Must have at least one row
and one column. Non-\code{data.frame} inputs are coerced.}

\item{partition}{Optional character vector of length \code{ncol(data)}
giving a partition label for each column. Dropping is performed
independently within each partition.}

\item{n_drop}{Integer scalar. Number of items to remove in each
partition.}

\item{direction}{Character string, \code{"tail"} (default) or \code{"head"}.
\code{"tail"} drops the weakest items; \code{"head"} drops the strongest.}

\item{criterion}{Character string, \code{"alpha"} (Cronbach’s alpha)
or \code{"lambda"} (CFA loadings).}

\item{approach}{Character string, \code{"oneshot"} (single pass)
or \code{"greedy"} (iterative dropping and refitting).}

\item{output_type}{One of \code{"names"} (default), \code{"subset"}, \code{"both"},
or \code{"debug"}.
\itemize{
\item \code{"names"} – character vector of dropped item names.
\item \code{"subset"} – reduced data frame with dropped columns removed.
\item \code{"both"} – list with elements \code{names} and \code{subset}.
\item \code{"debug"} – as \code{"both"} but also includes all captured messages,
warnings, and errors.
}}

\item{alpha_metric}{Character string. Cronbach’s alpha metric to
optimise (passed to \code{psych::alpha$alpha.drop}).}

\item{alpha_args}{Named list of extra arguments for
\code{\link[psych]{alpha}}.}

\item{measurement_model}{Optional character string containing a
\link[lavaan]{model.syntax} specification. If \code{NULL}, a single-factor
model with all items loading on one latent factor is used.}

\item{target_factor}{Character scalar giving the name of the factor
whose loadings determine item ranking when multiple factors are
present.}

\item{lambda_metric}{Character string indicating which solution matrix
to extract from \code{\link[lavaan]{inspect}}
(e.g., \code{"est"}, \code{"std"}, \code{"std.lv"}, \code{"std.nox"}, \code{"std.all"}).}

\item{cfa_args}{Named list of additional arguments passed to
\code{\link[lavaan]{cfa}}.}

\item{verbose}{Logical; if \code{TRUE} (default) prints a structured,
color-formatted report of all messages, warnings, and errors
captured during the run.}
}
\value{
If \code{output_type != "debug"}, returns either a character vector, data frame,
or list depending on the specified output type.
If \code{output_type = "debug"}, returns a list with components:
\describe{
\item{result}{The main result (or \code{NULL} if an error occurred).}
\item{warnings}{Character vector of captured warnings.}
\item{messages}{Character vector of informational messages.}
}
}
\description{
Removes the weakest (or strongest) items from a psychometric scale by either
(a) minimizing the decrease in Cronbach’s alpha or
(b) selecting the smallest absolute CFA loadings.
The function can operate on the full data set or within
user-defined column partitions.
}
\details{
\itemize{
\item Input validation is strict: missing or duplicated column names are
fixed or rejected, and each partition must contain at least \code{n_drop}
items.
\item Arguments that are not applicable to the chosen method (e.g.,
\code{alpha_args} when \code{criterion = "lambda"}) are detected automatically
and reported once via an informational message.
\item All messages, warnings, and errors from downstream calls are
captured and printed together at the end when \code{verbose = TRUE},
formatted using the internal helper \code{colormsg()}.
}
}
\section{Methods}{

\itemize{
\item \strong{Alpha} – ranks items using \code{\link[psych]{alpha}}, dropping those
whose removal least reduces Cronbach’s alpha (a measure of internal
consistency that equals true reliability when essential tau-equivalence
holds).
\item \strong{Lambda} – fits a one-factor CFA via
\code{\link[lavaan]{cfa}} and ranks items by absolute standardized
loadings.
}
}

\seealso{
\code{\link[psych:alpha]{psych::alpha()}}, \code{\link[lavaan:cfa]{lavaan::cfa()}}, \code{\link[lavaan:lavInspect]{lavaan::inspect()}},
and the internal helpers documented at \link{miscutils}.
}
