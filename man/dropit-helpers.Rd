% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/droputils.R
\name{dropit-helpers}
\alias{dropit-helpers}
\alias{greedydrop_lambda}
\alias{greedydrop_alpha}
\alias{oneshotdrop_lambda}
\alias{oneshotdrop_alpha}
\title{Internal Helper Functions for Item Dropping}
\usage{
greedydrop_lambda(dta, n_drp, dir, out, mmt_mdl, tgt_fct, lam_mtr, cfa_args)

greedydrop_alpha(dta, n_drp, dir, out, alp_mtr, alp_args)

oneshotdrop_lambda(dta, n_drp, dir, out, mmt_mdl, tgt_fct, lam_mtr, cfa_args)

oneshotdrop_alpha(dta, n_drp, dir, out, alp_mtr, alp_args)
}
\arguments{
\item{dta}{A \code{data.frame} of item responses (rows = respondents,
columns = items).}

\item{n_drp}{Integer scalar giving the number of items to remove.}

\item{dir}{Character string, either \code{"tail"} or \code{"head"}, passed to
\code{\link[utils:head]{utils::tail()}} / \code{\link[utils:head]{utils::head()}} to select the weakest or strongest
items.}

\item{out}{Character string, one of \code{"names"}, \code{"subset"}, or \code{"both"},
determining the type of object returned.}

\item{mmt_mdl}{Character string of lavaan model syntax for the CFA.
If \code{NULL}, a single-factor model is created automatically.}

\item{tgt_fct}{Character scalar giving the name of the target latent
factor used for ranking.  Required when the fitted CFA has multiple
factors.}

\item{lam_mtr}{Character string passed to
\code{\link[lavaan:lavInspect]{lavaan::inspect()}} selecting which standardized solution matrix
to extract (e.g., \code{"std"}).}

\item{cfa_args}{Named list of additional arguments passed to
\code{\link[lavaan:cfa]{lavaan::cfa()}}.}

\item{alp_mtr}{Character string naming the column of
\code{psych::alpha$alpha.drop} used for ranking.}

\item{alp_args}{Named list of additional arguments passed to
\code{\link[psych:alpha]{psych::alpha()}}.}

\item{crt}{Character string, \code{"alpha"} or \code{"lambda"},
indicating the ranking criterion.}

\item{apr}{Character string, \code{"oneshot"} or \code{"greedy"},
selecting the removal strategy.}
}
\value{
Depending on \code{out}:
\itemize{
\item \code{"names"} – character vector of dropped item names.
\item \code{"subset"} – reduced \code{data.frame} with the dropped items removed.
\item \code{"both"} – list with elements \code{names} and \code{subset}.
}
}
\description{
Low-level utilities used internally by \code{\link[=dropit]{dropit()}} to implement the
different item–removal strategies.
These functions are \strong{not} intended for direct use by end-users but
are documented for developers who may wish to extend or debug the
algorithm.
}
\details{
\itemize{
\item Alpha methods call \code{\link[psych:alpha]{psych::alpha()}} with \code{check.keys = TRUE}.
\item Lambda methods call \code{\link[lavaan:cfa]{lavaan::cfa()}} (defaulting to \code{std.lv = TRUE}
unless overridden) and extract the \code{"lambda"} matrix from
\code{\link[lavaan:lavInspect]{lavaan::inspect()}}.
\item All helpers validate that the number of extracted loadings matches
the number of columns in \code{dta} and throw an error otherwise.
}
}
\section{Helper Overview}{

\describe{
\item{naivedrop()}{Dispatch wrapper that calls the correct algorithm
based on \code{criterion} (\code{"alpha"} or \code{"lambda"}) and
\code{approach} (\code{"oneshot"} or \code{"greedy"}).}
\item{greedydrop_alpha()}{Iterative (“greedy”) removal using
Cronbach’s alpha.}
\item{oneshotdrop_alpha()}{Single–pass removal using Cronbach’s alpha.}
\item{greedydrop_lambda()}{Iterative (“greedy”) removal using CFA
factor loadings.}
\item{oneshotdrop_lambda()}{Single–pass removal using CFA
factor loadings.}
}
}

\seealso{
\code{\link[=dropit]{dropit()}}, \code{\link[psych:alpha]{psych::alpha()}}, \code{\link[lavaan:cfa]{lavaan::cfa()}}
}
\keyword{internal}
